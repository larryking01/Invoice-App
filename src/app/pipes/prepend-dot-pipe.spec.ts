import { PrependDotPipe } from './prepend-dot-pipe';

describe('PrependDotPipe', () => {
  it('create an instance', () => {
    const pipe = new PrependDotPipe();
    expect(pipe).toBeTruthy();
  });
});
